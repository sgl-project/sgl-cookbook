# Ring-2.5-1T Model Configurations (Simplified Format)
# This file is compiled to data/models/generated/dev-pr-18598/ring25.yaml
#
# GPU requirements:
#   H200: tp=8, max-running-requests=64
#   B200: tp=8

vendor: inclusionai

defaults:
  hardware:
    H200: { tp: 8 }
    B200: { tp: 8 }
  configurations:
    - name: default
      nodes: single
      optimization: balanced

families:
  - name: Ring-2.5
    description: Ring-2.5-1T trillion-parameter reasoning model with hybrid linear attention

    models:
      - name: Ring-2.5-1T
        quantization: fp8
        extra_args:
          - --trust-remote-code
        hardware:
          H200:
            tp: 8
            extra_args:
              - --max-running-requests
              - "64"
          B200:
            tp: 8
